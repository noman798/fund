#! /usr/bin/env coffee

wapi = require("./_wapi.coffee")

wapi.get(
    ".settings/rules", (error, res, body)->
        console.log JSON.stringify(
            JSON.parse(body)
            null
            4
        )
)

